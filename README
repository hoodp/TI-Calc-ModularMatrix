Program that multiplies two matrices together and finds its modulo based on the users input. The product is stored in [C] and the modulo is stored in [D]. Two valid matrices must be in [A] and [B] for the program to work correctly.
